<h1>Products</h1>

<table>
  <thead>
    <tr>
      <!-- More attributes here... -->
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %> </td> 
      </tr>
    <% end %>
  </tbody>
</table>